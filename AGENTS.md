# AGENTS.md

## 工作原则

- 修复根因。除非用户明确要求，不用临时补丁、静默回退或无关重构掩盖问题。
- 先阅读相关代码，再修改。优先复用现有组件和依赖，只为具体问题增加抽象，不为缩短文件而拆分模块。
- 修改聚焦目标，同时删除失效代码；支持已发布版本升级所需的迁移代码不属于失效代码。
- 文档保持简洁、有效。删除过时计划和报告，移动或重命名文档时修复链接；历史信息交给 Git。
- 仓库文本使用 LF 换行；必要的格式例外写入 `.gitattributes`。

## 设计边界

- 原生使用 LangChain、LangGraph 和 Deep Agents，不在应用层重复实现其执行循环、checkpoint 历史、恢复或调度。保持单一运行时和唯一会话状态来源，遵循[存储设计](docs/CURRENT_STATE_STORAGE.md)。
- 修改安全或审批行为前，先读[安全模型](docs/SECURITY_MODEL.md)。模型和用户选择的供应商是可信协作者，不自行引入恶意模型假设或普遍审批、过滤规则；保留校验、取消、资源限制和恢复能力。
- UI 复用共享组件、样式和设计变量。重复交互应改进共享实现，不增加局部变体。
- 配置修改须贯通默认值、持久化结构、类型、IPC 和界面。持久化键使用 `snake_case`，TypeScript 设置使用 `camelCase`。
- 可复用 Skills 保持跨宿主通用，使用标准元数据，不依赖 Anas 专属能力；内置应用配置 Skill 除外。宿主的技能可用性和来源设置放在应用配置中。

## 发布兼容性

- 首个公开的 `3.0.0` 版本是兼容基线。后续版本必须保留受支持版本的用户数据、配置和备份恢复能力，支持跳过中间版本升级。此前未发布的开发格式无需兼容，除非用户要求。
- 持久化或框架序列化格式改变时，提供有版本管理的显式迁移，转换到当前结构。保留用户选择和引用关系，不得静默重置数据、伪造已完成结果或在迁移中重放外部操作。
- 迁移须可安全重试，并通过备份、事务和暂存写入支持失败恢复。遇到不支持的新格式，应在修改数据前拒绝；不承诺降级读取。
- 用已发布格式的样本验证升级及失败恢复。未经用户明确同意，不删除受支持的升级路径；大版本升级也不例外。

## 文档语言

- `README.md` 默认中文，`README.en.md` 为英文，内容同步并提供互相切换的链接。
- 随包用户文档和 Release 说明提供中英双语；短文档可以在同一文件中并列。
- `AGENTS.md` 和其他项目文档使用简体中文。
- 模型指令（`SKILL.md`、技能参考资料、`MEMORY_RULES.md`）、许可证正文和上游声明保留原语言，不为双语要求重复模型指令。

## 版本与发布

- 首次源码快照推送到 `higale/Anas-public` 前，版本固定为 `3.0.0`；首次发布后删除这条临时规则。
- 此后每次提交应用修改时，除非用户另有要求，用 `npm version patch --no-git-tag-version` 递增一次 `PATCH`，同步两个 package 文件。使用三段版本号，只有用户明确要求时才变更 `MINOR` 或 `MAJOR`。仅修改文档、注释或 Agent 规则不升版本。
- 最终验证前确定提交范围和版本，按完整功能边界提交。公开仓库按[发布流程](docs/SOURCE_PUBLISHING.md)接收独立源码快照，不公开日常开发历史。

## 验证与交付

- 代码修改运行相关行为测试和 `npm run typecheck`；主进程与渲染进程集成变更运行 `npm run build`。纯文档修改检查链接和格式，无需重建应用。
- 测试可观察行为和有意义的失败边界，不锁死源码拼写或偶然的实现细节，不编写仅复述实现的测试。
- 视觉修改尽可能启动应用检查。涉及启动响应性时，使用实际 Electron 待处理操作检查（`npm run test:e2e -- --startup-only`），不能仅凭单元测试下结论。
- 交付时说明改动、已完成的验证和剩余限制，未执行的检查不得声称通过。
